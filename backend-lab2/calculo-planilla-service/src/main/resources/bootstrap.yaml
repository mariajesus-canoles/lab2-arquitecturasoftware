spring:
  application:
    name: calculo-planilla-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/calculo-planilla
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update