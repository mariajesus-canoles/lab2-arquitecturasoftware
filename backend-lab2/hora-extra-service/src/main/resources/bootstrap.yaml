spring:
  application:
    name: hora-extra-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/hora-extra
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update