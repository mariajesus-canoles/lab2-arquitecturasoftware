spring:
  application:
    name: justificativo-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/justificativo
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update