spring:
  application:
    name: pago-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/pago
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update