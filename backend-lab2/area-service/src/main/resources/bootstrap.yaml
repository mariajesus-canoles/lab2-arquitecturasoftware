spring:
  application:
    name: area-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/area
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update